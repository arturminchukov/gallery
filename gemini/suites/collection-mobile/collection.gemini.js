const suite = require('../../suite');
// let Gemini = require('gemini/api'),
//     gemini = new Gemini('.gemini.js');

// console.log(gemini.config.rootUrl);

suite({
    data: 'collection-mobile/default',
    selector: '.Pictures',
});
